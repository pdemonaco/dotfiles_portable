#!/bin/ksh
# Activate Aliases 
ALIASES="${HOME}/.config/aliases"
if [[ -r "${ALIASES}" ]]
then
    . "${ALIASES}"
fi

# Activate Colors
COLORS_CONFIG="${HOME}/.config/colors"
if [[ -r "${COLORS_CONFIG}" ]]
then
    . "${COLORS_CONFIG}"
fi

# Ensure VI mode is enabled
set -o vi
